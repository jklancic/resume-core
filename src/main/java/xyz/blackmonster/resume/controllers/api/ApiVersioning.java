package xyz.blackmonster.resume.controllers.api;

/**
 * API versioning paths. <p>Note the root path is already set to /api/ in the YAML configuration.</p>
 */
public class ApiVersioning {
	
	public static final String API_V1 = "/v1";
}
